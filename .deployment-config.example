# Deployment Configuration
# Copy this file to .deployment-config and fill in your values
# This file is sourced by the deployment scripts

# REQUIRED: Path to your Concrete CMS site root (where composer.json, public/, etc. are located)
# This should be an absolute path
SITE_PATH=""  # e.g., /home/user/site or /var/www/html

# Remote server configuration (for deploy-to-production.sh)
REMOTE_HOST=""  # e.g., user@example.com
REMOTE_PATH=""  # e.g., /var/www/html (path to site on production server)
REMOTE_USER=""  # Optional: SSH user (will be combined with REMOTE_HOST if not included)

# Production database credentials (for automatic database sync)
# WARNING: This file may contain sensitive information - do not commit to Git!
PROD_DB_HOST=""  # Production database hostname
PROD_DB_NAME=""  # Production database name
PROD_DB_USER=""  # Production database username
PROD_DB_PASS=""  # Production database password (consider using environment variable instead)

# File syncing configuration (for Git method)
FILES_GIT_REPO=""  # e.g., git@github.com:user/files-repo.git
FILES_GIT_BRANCH="main"  # Branch to use for files

# Uploaded files sync behavior
SYNC_UPLOADED_FILES="auto"  # Options: auto, ask, skip

# Uploaded files transfer method
UPLOADED_FILES_METHOD="git"  # Options: git, zip, rsync

# Database sync behavior
SYNC_DATABASE="auto"  # Options: auto, skip (auto = sync automatically, skip = never sync)
