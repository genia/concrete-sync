# Deployment Configuration
# Copy this file to .deployment-config and fill in your values
# This file is sourced by the deployment scripts

# REQUIRED: Path to your Concrete CMS site root (where composer.json, public/, etc. are located)
# This should be an absolute path
SITE_PATH=""  # e.g., /home/user/site or /var/www/html

# REQUIRED: Environment type - set to "prod" on production server, "dev" on development machine
ENVIRONMENT=""  # Options: "prod" or "dev"

# Database credentials (for the database on THIS host)
# On dev: these access your local development database
# On production: these access the production database
# WARNING: This file may contain sensitive information - do not commit to Git!
DB_HOSTNAME=""  # Database hostname (e.g., localhost or mysql.example.com)
DB_DATABASE=""  # Database name
DB_USERNAME=""  # Database username
DB_PASSWORD=""  # Database password (consider using environment variable instead)

# File syncing configuration (REQUIRED - Git method only)
FILES_GIT_REPO=""  # REQUIRED: e.g., git@github.com:user/files-repo.git
FILES_GIT_BRANCH="main"  # Branch to use for files

# Composer configuration (OPTIONAL)
# If your system has composer.phar in a specific location (not in PATH), set this
# Leave empty to use system 'composer' command (must be in PATH)
COMPOSER_DIR=""  # e.g., /usr/local/bin or /opt/composer (directory containing composer.phar)
# When set, the script will use: php ${COMPOSER_DIR}/composer.phar
# When not set, the script will use: composer (system command)

# Uploaded files sync behavior
SYNC_UPLOADED_FILES="auto"  # Options: auto, ask, skip

# Database sync behavior
SYNC_DATABASE="auto"  # Options: auto, skip (auto = sync automatically, skip = never sync)
